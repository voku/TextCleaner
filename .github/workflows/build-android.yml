name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon

      - name: Rename APK
        run: cp android/app/build/outputs/apk/debug/app-debug.apk TextCleaner.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: TextCleaner-debug-apk
          path: TextCleaner.apk
          retention-days: 30

      - name: Publish rolling latest release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: android-latest
          name: Android – latest build
          prerelease: true
          body: |
            Automatically built from the latest commit on `main`.

            **Install:** enable *Install unknown apps* on your device, download `TextCleaner.apk`, and open it.
          files: TextCleaner.apk
