# Default ProGuard rules for TextCleaner.
# The core cleanup logic uses only standard Kotlin APIs and no reflection,
# so the default R8 optimisation is sufficient.

-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
