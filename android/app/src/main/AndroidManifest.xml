<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TextCleaner">

        <!-- Full-screen launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TextCleaner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Translucent overlay activity.
            windowIsTranslucent keeps the originating app visible behind the Compose scrim,
            so the user never leaves their current context.
            taskAffinity="" + excludeFromRecents prevent it from polluting the recents stack.
        -->
        <activity
            android:name=".OverlayActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TextCleaner.Overlay"
            android:taskAffinity=""
            android:excludeFromRecents="true">

            <!-- PROCESS_TEXT: appears in text-selection context menu -->
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Share target: receive text via share sheet -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Clean clipboard, fired by CleanerTileService -->
            <intent-filter>
                <action android:name="com.voku.textcleaner.ACTION_CLEAN_CLIPBOARD" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Quick Settings tile: "Clean clipboard" -->
        <service
            android:name=".CleanerTileService"
            android:exported="true"
            android:label="@string/tile_label"
            android:icon="@drawable/ic_tile"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>
</manifest>
